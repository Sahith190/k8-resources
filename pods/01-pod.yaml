apiVersion: v2
kind: Pod
metadata:
  name: simple-Pod
  Namespace: roboshop # if we dont give specific namespace it will take default namespac
spec: 
  containers:
  - name: nginx-simple
    image: nginx:1.14.2
    ports:
    - containerPort: 80